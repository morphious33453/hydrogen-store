{
    "buildCommand": "cross-env DEPLOY_TARGET=vercel shopify hydrogen build --codegen && rm -rf build && mv dist build",
    "devCommand": "npm run dev",
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
    "framework": "remix",
    "outputDirectory": "build/client"
}